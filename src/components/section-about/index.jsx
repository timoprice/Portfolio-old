import React from 'react';

import Section from '../section';

const SectionAbout = ({ about }) => {
  return (
    <Section title="About Me">
      <div className="mb-6">
        <p>{about}</p>
      </div>
    </Section>
  );
};

const SectionAbout = ({ about2 }) => {
  return (
    <Section title="About Me">
      <div className="mb-6">
        <p>{about2}</p>
      </div>
    </Section>
  );
};

const SectionAbout = ({ about3 }) => {
  return (
    <Section title="About Me">
      <div className="mb-6">
        <p>{about3}</p>
      </div>
    </Section>
  );
};

const SectionAbout = ({ about4 }) => {
  return (
    <Section title="About Me">
      <div className="mb-6">
        <p>{about4}</p>
      </div>
    </Section>
  );
};

export default SectionAbout;
